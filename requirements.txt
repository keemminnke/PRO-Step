# Core
torch>=2.1.0
transformers>=4.40.0
numpy>=1.24.0

# Inference
vllm>=0.14.0
# flash-attn: install separately (requires matching CUDA/torch version)
#   pip install flash-attn --no-build-isolation

# Training
peft>=0.18.0
datasets>=3.0.0
accelerate>=1.0.0
trl>=0.29.0

# Retrieval
faiss-cpu>=1.7.0
rank-bm25>=0.2.2

# Evaluation
flashrag
scikit-learn>=1.3.0

# Utilities
tqdm>=4.66.0
pyyaml>=6.0
